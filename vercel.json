{
  "buildCommand": "vite build && node -e \"require('fs').copyFileSync('index.html', 'dist/client/index.html')\"",
  "outputDirectory": "dist/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
